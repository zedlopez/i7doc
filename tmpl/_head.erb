<!doctype html>
<html lang="en"><head><meta charset="utf-8">
<title><%= title %></title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Public+Sans:wght@300..700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="<%= ((['..']*level)+['i7doc.css']).join('/') %>">
<% if !stylesheets.blank? %>
<% stylesheets.each do |stylesheet| %>
<link rel="stylesheet" href="<%= ((['..']*level)+[stylesheet]).join('/') %>">
<% end %>
<% end %>
<% if !js.blank? %>
<% js.each do |js| %>
<script src="<%= ((['..']*level)+[js]).join('/') %>"></script>
<% end %>
<% end %>
</head>
